const initialState = {
    group: {},
    rest: {},
    dashboard: {},
    show: false,
    display: {},
    images: ''
};

export default (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};